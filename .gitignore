# Dart / Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# NOTE: generated Isar code (lib/**/*.g.dart) is intentionally committed so the
# project builds without running build_runner. Regenerate with:
#   dart run build_runner build --delete-conflicting-outputs

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store

# Isar local db (created at runtime)
*.isar
*.isar.lock
